services:
  api:
    build: .
    ports:
      - "8001:8080"
    env_file:
      - .env
    volumes:
      - .:/app
      - ./models:/app/models

    command: uvicorn app.main:app --host 0.0.0.0 --port 8080 --reload